# Schema parity fixtures

Plan §4 M1. JSON schemas for the 11 MCP tools of `codebase-memory-mcp`, extracted from its README + a recorded session. CI runs schema-compat tests against these.

Land in M1 week 5 with the rmcp server.
