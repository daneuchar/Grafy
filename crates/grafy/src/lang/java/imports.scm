; Grafy M1 W3 — Java imports.
; @import.name   — the simple class/member name (last segment)
; @import.module — the full import path

; import foo.bar.Baz;
(import_declaration
  (scoped_identifier) @import.module)
